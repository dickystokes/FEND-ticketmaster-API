import React from "react";
import App from "../App";

const Search = props => {
  return (
    <form>
      <input type="text" />
      <input type="submit" />
    </form>
  );
};

export default Search;
